apiVersion: kubeflow.org/v1beta1
kind: Profile
metadata:
  name: os4ml   # replace with the name of profile you want, this will be user's namespace name
spec:
  owner:
    kind: User
    name: tk@wogra.com   # replace with the email of the user
