apiVersion: v1
kind: Service
metadata:
  name: jobmanager
spec:
  type: ClusterIP
  ports:
    - port: 8000
      protocol: TCP
      targetPort: http
      name: api
  selector:
    app: os4ml
    name: jobmanager