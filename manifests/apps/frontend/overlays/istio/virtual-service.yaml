apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: os4ml-ui
spec:
  gateways:
    - kubeflow/kubeflow-gateway
  hosts:
    - '*'
  http:
    - name: "os4ml-frontend-route"
      match:
        - uri:
            prefix: /os4ml/
      rewrite:
        uri: /
      route:
        - destination:
            host: os4ml-ui.os4ml.svc.cluster.local
            port:
              number: 80
