apiVersion: v1
kind: Service
metadata:
  name: ui
  labels:
    app: os4ml
  namespace: os4ml
spec:
  selector:
    app: os4ml
    tier: frontend
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
  type: ClusterIP
